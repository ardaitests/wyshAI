{
  "name": "wyshai-scheduler",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-to-docs",
    "preview": "vite preview",
    "copy-to-docs": "rm -rf docs && mkdir -p docs && cp -r build/* docs/"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.4.1",
    "vite": "^5.0.12"
  }
}
